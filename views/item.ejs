<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= item.name %> Details</title>
  </head>
  <body>
    <h1>Item Details: <%= item.name %></h1>
    <p><strong>Description:</strong> <%= item.description %></p>
    <p><strong>Quantity:</strong> <%= item.quantity %></p>
    <p><strong>Price:</strong> $<%= item.price.toFixed(2) %></p>

    <nav>
      <a href="/categories/<%= categoryId %>/items">Back to Items</a>
      <a href="/categories/<%= categoryId %>/items/<%= item.id %>/update">Update Item</a>
    </nav>

    <form
      action="/categories/<%= categoryId %>/items/<%= item.id %>"
      method="POST"
      style="display: inline"
    >
      <button
        type="submit"
        onclick="return confirm('Are you sure you want to delete this item?');"
      >
        Delete Item
      </button>
    </form>
  </body>
</html>
