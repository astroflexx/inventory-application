<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Items in Category</title>
  </head>
  <body>
    <h1>Items</h1>
    <nav>
      <a href="/categories">Back to Categories</a>
      <a href="/categories/<%= categoryId %>/items/new">Add New Item</a>
    </nav>

    <h2>Item List</h2>
    <ul>
      <% if (items.length > 0) { %> <% items.forEach(item => { %>
      <li>
        <strong>
          <a href="/categories/<%= categoryId %>/items/<%= item.id %>">
            <%= item.name %>
          </a>
        </strong>
        <a href="/categories/<%= categoryId %>/items/<%= item.id %>/update">Update</a>
        <form
          action="/categories/<%= categoryId %>/items/<%= item.id %>"
          method="POST"
          style="display: inline"
        >
          <button
            type="submit"
            onclick="return confirm('Are you sure you want to delete this item?');"
          >
            Delete
          </button>
        </form>
      </li>
      <% }); %> <% } else { %>
      <li>No items available in this category.</li>
      <% } %>
    </ul>
  </body>
</html>
